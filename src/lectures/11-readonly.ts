/* ReadonlyArray is used to make an array immutable. This also means that we can only use
immutable array methods like map, forEach, filter, reduce, etc. */
const numbers: ReadonlyArray<number> = [1,2,3,4];
